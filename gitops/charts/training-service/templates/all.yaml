{{- define "training-service.labels" -}}
app: training
{{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-deployment
  labels:
    {{- include "training-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "training-service.labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "training-service.labels" . | nindent 8 }}
    spec:
      containers:
        - name: training-cont
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: training-config
            - secretRef:
                name: training-secret
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: training-svc
  labels:
    {{- include "training-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    {{- include "training-service.labels" . | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: training-config
data:
  PORT: "{{ .Values.service.port }}"
  NODE_ENV: "production"
---
apiVersion: v1
kind: Secret
metadata:
  name: training-secret
type: Opaque
stringData:
  MONGO_URI: {{ .Values.global.mongo.uri | quote }}
  JWT_SECRET: {{ .Values.global.jwtSecret | quote }}
